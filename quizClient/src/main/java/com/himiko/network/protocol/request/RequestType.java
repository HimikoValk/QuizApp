package com.himiko.network.protocol.request;

public enum RequestType {
    USER_LOGIN,
    USER_LOGOUT,
    GET_GAMES,
    GET_GAME_INFO,
    GAME_CREATE,
    GAME_JOIN,
    GAME_LEAVE,
    GAME_EDIT,
    SERVER_INFORMATION,
}
